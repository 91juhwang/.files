alias subl="/Applications/Sublime\ Text.app/Contents/SharedSupport/bin/subl"

[[ -s "$HOME/.profile" ]] && source "$HOME/.profile" # Load the default .profile

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*

alias served="ruby -run -e httpd . -p 5000"

export QUOTE_API_KEY=BjQC3vbzEZmshLm52Tkf60otAvDpp1dhB9Jjsn5CHyklJdcNDl
export RIOT_API_KEY=RGAPI-3DA52C57-BB87-4FA4-A4B4-32CC2E7E68FE
export SENDGRID_API_KEY=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
export PATH=/opt/local/bin:$PATH

# Load the shell dotfiles, and then some:
# * ~/.path can be used to extend `$PATH`.
# * ~/.extra can be used for other settings you don’t want to commit.
for file in ~/.{path,bash_prompt,exports,aliases,functions,extra}; do
  [ -r "$file" ] && source "$file"
done
